// OmbraSELib.cpp - Stub source file for static library generation
//
// This file exists solely to ensure the OmbraSELib project produces a valid
// .lib output. The actual functionality is in the headers (UEFI-compatible
// inline functions and macros).
//
// SELib in Sputnik was a compiled library; we replicate that pattern here
// even though most functionality is header-only for UEFI compatibility.
//
// The UEFI global service pointers (gBS, gRT, gST, gImageHandle) are provided
// by the EDK2 libraries linked by the main Ombra project, not defined here.

#include "OmbraSELib.h"

// Placeholder to ensure the library compiles with at least one translation unit
namespace OmbraSELib {
    // Reserved for future compiled functionality
}
